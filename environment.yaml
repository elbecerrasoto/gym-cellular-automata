name: gymca
channels:
  - conda-forge
dependencies:
# Dependencies
  - gym
  - numpy
  - matplotlib
  - scipy
  - seaborn
  - pyyaml
# Development
  - pip
  - black
  - isort
  - mypy
  - pytest
  - pytest-repeat
  - pytest-cov
  - pre-commit
  - ipdb
  - pip:
    # Dependencies
    - svgpath2mpl
    # Development
    - pytest-randomly
    # pip install git+git://github.com/mverteuil/pytest-ipdb.git
    - pytest-ipdb
